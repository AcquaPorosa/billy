#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#ifdef _DEBUG
	#define DEBUG_TRACE_EXECUTION
	//#define DEBUG_PRINT_CODE
#endif // DEBUG

#define UINT8_COUNT (UINT8_MAX + 1)
